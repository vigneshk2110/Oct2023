
// Null Pointer 

#include <stdio.h>
int main() {
    char *alphabetAddress = NULL; /* Null pointer */

    printf("%c", *alphabetAddress);

// A null pointer points at nothing, or at a memory address that users can not access.

}